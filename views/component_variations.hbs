{{#extend "iframe"}}
  {{#content "body"}}
    {{#if cssFile}}
      <link rel="stylesheet" href="{{cssFile}}">
    {{/if}}
    {{#if jsFile}}
      <script src="{{jsFile}}"></script>
    {{/if}}
    <div class="RoundupComponents">
      {{#each variations}}
        <div class="RoundupComponent">
          <a href="?component={{this.file}}&variation={{this.variation}}" class="RoundupComponent-file">{{this.variation}}</a>
          <div class="RoundupComponent-html">
            {{{this.html}}}
          </div>
        </div>
      {{/each}}
    </div>
  {{/content}}
{{/extend}}
