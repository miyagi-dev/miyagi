{{#extend "iframe"}}
  {{#content "body"}}
    <div class="FreitagComponents">
      {{#if components}}
        {{#each components}}
          <div class="FreitagComponent">
            <a href="/component?file={{this.file}}&embedded=true" class="FreitagComponent-file">{{this.file}}</a>
            <div class="FreitagComponent-html">
              {{{this.html}}}
            </div>
          </div>
        {{/each}}
      {{else}}
        <div class="FreitagComponent">
          There are no components yet.
        </div>
      {{/if}}
    </div>
  {{/content}}
{{/extend}}
